# Backup Ignore Files
.DS_Store
Thumbs.db
*.pyc
*.py[cod]
*$py.class
__pycache__/
*.log
temp/
tmp/
*.bak
*.backup
.vscode/
.idea/
node_modules/

# social-clip 优化计划:本地留档,不入库
social-clip/OPTIMIZATION_PLAN.md

# 盲检委托产物(delegate_review.py 运行时生成,属项目工作目录,不应进技能库)
.review_pkg_*.json
.review_return_*.json

# 各技能开发期回归测试：只留本机(scripts/ 原地可跑)，不随技能分发给用户
*/scripts/test_*.py

# 运行时心跳（hook 每次介入都会写，不入库）。_shared/ 那份此前被跟踪，每跑一次
# hook 就把工作区弄脏，极易被顺手 commit 进去 —— 已 git rm --cached 摘除。
# 🔴 2026-08-03 改为通配：原先只挡这两个具体路径，但心跳会写进**任何**技能的
# scripts/ 下（实测 review-writing/scripts/hook_heartbeat.json 就漏在外面）。
# 内容形如 {"skill":"review-writing","file":"drafts/section_1.md",...}，
# 即用户正在写哪份稿的运行时记录——既不该入库，也不该随 rsync 进分发镜像。
# 只列具体路径是"每来一个新落点补一条"的散弹写法，通配才是根因修法。
hook_heartbeat.json

# 环境自检运行时产物（env_preflight.py 每跑一次重写），不入库也不该随技能分发。
# 🔴 2026-08-04 改为裸文件名：原先写 `*/env_status.json` 只匹配**一层**目录，而实际
# 落点是 `<技能>/scripts/env_status.json`（两层），于是 gsw 那份一直漏在外面、随时会被
# git add 误收。这和心跳文件是同一个坑的第二次发作（那次是"只列具体路径"，这次是
# "通配层级不够"）。裸文件名在 gitignore 里匹配任意深度，一次到位。
# 同理，心跳那几条也一并收敛成裸名，别再按层级写。
env_status.json
